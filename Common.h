#ifndef COMMON_H
#define COMMON_H

enum {
    COMMON, // common type for command operation.
    STRING, // string type for key-value.
    LIST, // list type for key-value.
    DICT // dict type for key-value.
};

#endif
